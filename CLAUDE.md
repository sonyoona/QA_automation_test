# CLAUDE.md

이 저장소에서 QA 자동화 테스트를 설계·작성할 때 지켜야 할 규칙입니다.
"어떤 화면을 어떻게 검증하는지"는 `docs/notes/code-notes/`의 화면별 노트를 보고,
여기는 화면이 바뀌어도 계속 적용되는 **공통 설계 원칙**만 담습니다.

## 스택 & 실행

- Python + pytest + Playwright(sync API), 설정은 `.env` (`.env.example` 참고)
- 실행: `pytest -v` / 특정 파일만 `pytest test_xxx.py -v`
- 최초 실행 시 OTP 입력을 위해 headed 브라우저가 뜸 (`auth.json`에 세션 저장 후 재사용)
- **pytest를 동시에 두 개 이상 돌리지 않는다.** 서버가 계정당 단일 세션만 허용해서, 두 번째 프로세스가 로그인하는 순간 첫 번째 세션이 끊긴다. 같은 계정으로 브라우저에 로그인해 있어도 마찬가지로 끊긴다. 연속으로 에러(`E`)가 쏟아지면 코드 문제보다 **세션 충돌을 먼저 의심**한다 (실제로 겪음 — 두 프로세스를 동시에 돌렸다가 25분 넘게 걸려 있었다).
- **콘솔에 출력하는 스크립트에는 cp949에 없는 문자(em dash `—`, 체크마크 `✓` 등)를 쓰지 않는다.** 한국어 Windows 콘솔은 기본 인코딩이 cp949라 그런 문자를 `print`하면 `UnicodeEncodeError`로 죽는다 (실제로 겪음 — `tools/postprocess_allure_results.py`의 `print`에 `—`를 썼다가 크래시했다). 아스키 문자(`-`)로 대체한다.

## 인증 — 항상 기존 fixture를 재사용

- 새 테스트에서 로그인을 직접 구현하지 않는다. `conftest.py`의 `logged_in_page` fixture를 받아쓴다.
- 세션당 로그인은 최대 1회(`auth_state`, session scope)면 충분하다 — 테스트마다 필요한 건 로그인이 아니라 **새 탭**(`logged_in_page`, function scope)이다.
- `auth.json`은 절대 커밋 금지 (`.gitignore`에 있음, 토큰 평문 포함).

## git — 기본 브랜치(`main`)에 직접 커밋 금지

**작업이 하나여도 브랜치를 만든다** (2026-09-08 부터). 그 전에는 전부 기본 브랜치에 직접
커밋했는데, 되돌릴 일이 생기면 이미 다른 작업과 섞여 있어서 그 커밋만 빼내기가 어렵다.

**기본 브랜치 이름은 `main` 이다** — 2026-09-08 에 `master` 에서 바꿨다. 옛 이름을 쓰는
문서·스크립트가 남아 있으면 함께 고친다.

- 브랜치명은 커밋 유형을 접두어로 쓴다 — `fix/…` · `feat/…` · `docs/…` · `refactor/…`
- **push 와 PR 은 매번 따로 확인받는다.** 저장소가 공개 GitHub 에 있어서, 한 번 밀면
  되돌려도 흔적이 남는다.
- 커밋 메시지 규칙은 `테스트 코드 컨벤션` 아래가 아니라 여기 — 제목은 `유형: 무엇을`,
  본문에는 **왜** 와 **다음 사람이 밟을 함정**을 적는다. 무엇을 바꿨는지는 diff 가 이미 말한다.

## 화면에 대한 사실은 실측으로만 단언한다

**기존 코드·주석·다른 화면의 경험은 근거가 아니다.** 2026-09-08 실측 — 결과 행의 액션 버튼을
코드가 "상세 버튼"이라 부르고 있어 그대로 승계했는데, 실제 라벨은 [수정]이었고 그나마 한 가지도
아니라 행 상태에 따라 [수정]/[상세]로 갈렸다. "수정"으로 단정하고 assert를 걸었으면 완료 건
4행에서 거짓 실패가 났다. 같은 날 "이 화면 상태 탭엔 건수 배지가 없다"고도 단언했는데 실제로는
`전체 591`처럼 붙어 있었다 — 배지가 비어 있던 것이 곧 "데이터가 안 왔다"는 신호였는데 정반대로
읽었다. **둘 다 화면을 5초 보면 안 틀릴 것이었다.**

| 실측이 필요한 때 | 알아내야 하는 것 |
|---|---|
| 새 화면·새 요소를 다룰 때 | 셀렉터와 DOM 구조 |
| 값이나 라벨을 `assert` 할 때 | 그 값의 실제 형태 (아이콘 텍스트가 섞여 오는지 포함) |
| 대기 조건을 정할 때 | 무엇이 언제 채워지는지 — 데이터 도착을 알리는 신호가 무엇인지 |
| "이게 화면의 무엇이냐"는 질문이 나올 때 | 그 질문이 나왔다는 것 자체가 실측을 안 했다는 신호다 |

**안 해도 되는 것** — 리팩터링, 문서 정리, 이미 실측해서 노트에 날짜와 함께 적힌 값의 재사용.

**실측 스크립트 규칙**
- **읽기 전용.** 클릭은 최소로 하고, 데이터를 바꾸는 버튼은 절대 누르지 않는다 (`데이터를 바꾸는 동작` 장).
- `test_probe_<화면>.py` 로 만들고 `logged_in_page` fixture 를 그대로 쓴다 — 로그인을 새로 짜지 않는다.
- **커밋하지 않는다.** 확인이 끝나면 파일째 지운다 — 남으면 `pytest -v` 전체 실행에 딸려 들어간다.
- 알아낸 값은 코드 주석과 code-notes 에 **날짜와 함께** 남긴다. 날짜가 없으면 다음 사람이 언제 기준인지
  몰라서 결국 다시 재게 된다.

**실행은 조율한다 — 누가 돌리는지가 종류마다 다르다** (2026-09-09 개정).
계정이 단일 세션이라 아무나 임의로 돌리면 **상대가 열어둔 화면의 세션이 끊긴다** (`스택 & 실행` 장).
그래서 "Claude 는 절대 안 돌린다" 로 두었었는데, 실측 스크립트까지 사람에게 넘기니
`화면 확인 -> 추측으로 고침 -> 또 틀림` 이 반복됐다. **묻고 돌리는 것**으로 바꾼다.

| 무엇을 | 누가 | 어떻게 |
|---|---|---|
| **실측 스크립트** (`test_probe_*`, codegen) | **Claude** | **돌리기 전에 "브라우저로 로그인해 둔 것 없나" 를 묻고, 확인받은 뒤 돌린다** |
| 브라우저를 안 여는 것 (`--collect-only`·`py_compile`·git·마커 확인) | Claude | 그냥 돌린다. 세션을 안 쓴다 |
| **전체 실행·리포트 생성** (`pytest -v`, allure) | **사용자** | Claude 는 명령만 제시한다. 20분 이상 걸려 그동안 화면을 못 쓴다 |
| **mutating 실행** (`--allow-mutating`) | **사용자** | 데이터를 바꾼다. 사람이 의도를 갖고 켜는 자리다 |

- **묻는 것을 생략하지 않는다.** "아마 안 열어놨겠지" 로 돌리면 상대 세션이 끊기고,
  증상이 `연속 E` 로 나타나 **코드 버그로 오인**된다 (`실행 전제와 실패 구분` 장).
- 돌린 뒤에는 **알아낸 값을 그 자리에서 노트에 날짜와 함께** 남긴다. 프로브는 지우므로
  기록이 없으면 다음 사람이 같은 것을 또 잰다.

## 셀렉터 & 대기 — 자주 깨지는 지점

- **부분 일치 함정**: `name="완료"`는 "배정완료"까지 잡는다. 시작을 고정해야 하면 `re.compile("^완료")`.
- **이름에 박힌 숫자**: `name="완료 217"`처럼 건수가 포함된 텍스트는 건수가 바뀌면 깨진다. 숫자는 빼고 정규식으로 앞부분만 고정.
- **`assert` vs `expect`**: 판단 기준은 "DOM이냐 아니냐"다. 브라우저 DOM 요소의 상태·텍스트·가시성처럼 **비동기로 늦게 뜨는 대상**은 항상 `expect`(재시도됨) 사용. 파이썬 변수·계산 결과·API 응답 객체처럼 **DOM과 무관한 값**은 `assert`. ("화면 검증=expect"로 기계적으로 넓히지 않는다 — 계산 결과까지 expect로 감쌀 필요 없음.)
- **`expect()`가 아닌 조회 메서드에는 재시도가 없다.** `is_visible()`·`count()`·`inner_text()`·`all_inner_texts()`는 **그 순간의 DOM을 그대로 읽는다.** 아직 안 그려진 화면을 읽으면 전부 "없음"·0건으로 나오는데, 그게 실패가 아니라 **통과**로 보일 수 있다(위 `위양성` 장). 여러 요소를 한 번에 훑어야 해서 이 메서드들을 써야 한다면, **먼저 대표 요소 하나를 `expect()`로 기다려 렌더 완료를 확인한 뒤** 나머지를 읽는다 (`test_field_service.py`의 `_find_hidden` 패턴).
- 스피너가 있는 화면은 액션 후 `.spinner`가 사라질 때까지 명시적으로 기다린다 (`wait_for(state="hidden")`). 기본 타임아웃(5초)로 부족하면 늘린다.
- **고정 대기(`time.sleep`, `page.wait_for_timeout`)를 새로 쓰지 않는다.** `click()`/`fill()` 같은 Locator 액션은 자동으로 기다리고, 상태 검증은 `expect()`가 재시도한다. 그걸로 표현 안 되는 조건만 `page.wait_for_function()`(브라우저 안에서 직접 평가해 왕복이 없음)을 쓴다.
- **"바뀌었나"를 `cur != prev`로만 판정하지 않는다.** 표를 다시 그리는 동안 잠깐 빈 목록이 되는데, `[] != prev`도 참이라 그 순간에 빠져나올 수 있다. **값이 있고 + 연속 2회 같을 때만** "안정됐다(=갱신 끝)"고 본다.
- **컬럼 위치·개수를 하드코딩하지 않는다.** 헤더 텍스트로 인덱스를 찾아내는 헬퍼(`_find_reseller_col_index` 패턴)를 만들어, 컬럼 순서가 바뀌어도 테스트가 안 깨지게 한다.
- **Locator 우선순위** — 사용자 관점에서 의미가 명확하고 변경 가능성이 낮은 것부터 쓴다. 동일 텍스트가 여러 개면 부모 영역으로 범위를 좁힌다.

  | 순위 | 방법 | 비고 |
  |---|---|---|
  | 1 | role / label (`get_by_role(name=...)`) | **사용자가 보는 이름**으로 잡는다. 라벨이 사라지면 실패하므로, 사용자가 겪는 것과 같은 것을 본다 |
  | 2 | `get_by_test_id()` | 1번으로 구분이 안 되거나 이름이 자주 바뀌는 자리. **화면에 안 보이는 속성이라, testid로 찾았다는 사실 자체는 "보인다"를 보장하지 않는다** — 노출 검증은 `expect(...).to_be_visible()`로 따로 한다 |
  | 3 | placeholder | 실제로 안정적인 경우에만 |
  | 4 | CSS / XPath | 다른 방법이 없을 때 최후 수단. **`# TODO(testid):` 주석을 함께 남겨** FE 요청 근거로 축적한다 |

  화면마다 상황이 다르다 — 현장 서비스 관리는 testid 59개가 붙어 있고, 모니터는 0개라 CSS(`div.column`, `.spinner`)로 잡고 있다. **한 화면 기준으로 "우리는 testid를 쓴다"고 일반화하지 않는다.**
  회사가 `data-qa` 같은 다른 속성명을 쓰면 제품 코드가 아니라 Playwright 쪽을 맞춘다 — `playwright.selectors.set_test_id_attribute("data-qa")` 한 줄.

## Page Object — 셀렉터가 사는 곳

**한 화면의 셀렉터는 `pages/<화면명>_page.py` 한 파일에만 적는다. 테스트 파일에는 셀렉터 문자열이 나오지 않는다.**
(견본: `pages/field_service_page.py` — 현장 서비스 화면의 testid 59개가 전부 여기 있고, `test_field_service.py`에는 `data-testid`라는 글자가 한 번도 안 나온다.)

```python
# 나쁨 — 테스트가 셀렉터를 직접 안다
dlg = page.get_by_test_id("fs-new__dialog")
dlg.get_by_test_id("fs-new__temp-reg-checkbox").check()

# 좋음 — 화면 객체가 자기 셀렉터를 안다. TC는 '무엇을'만 말한다
popup = fs.open_new_service_popup()
popup.temp_reg_checkbox.check()
```

- **요소는 `@property`로, 동작은 메서드로.** property는 호출할 때마다 Locator를 새로 만들어서, 화면이 다시 그려져도 낡은 참조를 들고 있지 않는다.
- **팝업·모달은 별도 클래스로 두고, 그 안의 요소는 `self.dialog` 기준으로 좁힌다.** 팝업은 페이지가 아니라 "그 화면 안에서 열리는 것"이고, 페이지 전체에서 찾으면 뒤에 깔린 목록의 같은 이름 요소를 잡을 수 있다.
- **화면 진입(`goto`)과 로딩 대기(`wait_loaded`)는 화면 객체가 갖는다.** 나중에 스피너가 생기면 `wait_loaded()` 한 곳만 고치면 모든 TC에 반영된다.
- **UI를 조작하는 메서드에는 `@allure.step`을 붙인다** (아래 Allure 컨벤션 참고).

**셀렉터 이름이 바뀔 때 한 곳만 고치면 된다는 건 약한 이유다.** 진짜 이유는 둘이다.

| | |
|---|---|
| **읽는 방법이 갈리지 않는다** | 화면 객체에 `row_ids()` 하나가 있으면 TC가 읽는 방법을 고를 여지가 없다. 흩어져 있으면 TC마다 다른 방법을 쓰게 되고, 그중 하나가 위양성이 되어도 **셀렉터 이름은 멀쩡해서 아무도 눈치채지 못한다** (위 `위양성` 장의 "같은 것을 읽는 방법은 하나여야 한다") |
| **셀렉터 전략이 바뀔 때 대비가 된다** | 지금 CSS로 잡아둔 곳에 나중에 testid가 붙으면 화면 객체의 그 한 줄만 바뀐다. Page Object가 없으면 흩어진 27곳을 찾아 바꾸다 한 곳을 놓친다 — **44곳만 고치고 1곳 놓치는 게 제일 나쁘다** |

### 언제 헬퍼·메서드로 빼는가

**반복 횟수만으로 판단하지 않는다.** 판단 기준은 넷이고, 두 번째가 제일 중요하다.

| 질문 | 답이 "예"면 |
|---|---|
| 3번 이상 반복되나 | 뺀다 |
| 그 조작에 **대기·재시도·값 안정화 같은 '요령'이 붙나** | **한 번만 쓰여도 반드시 뺀다** |
| 이 덩어리에 **이름을 붙일 수 있나** | 뺀다 |
| 그냥 한 줄인가 | **빼지 마라** |

`.check()` 한 줄을 감싸는 건 이름만 바꾼 것이라 이득이 없고, 오히려 한 단계 더 들어가서 읽기 나빠진다. 반대로 "버튼을 누른다"처럼 보이는 한 줄에 *활성화 대기 → 클릭 → 결과 행 증가 확인 → 씹히면 재시도*가 딸려 있다면, 그 지식이 TC 본문에 흩어지면 안 된다.

**아직 안 아픈데 미리 빼지 않는다.** 어떻게 쓰일지 모르는 상태에서 묶으면 잘못된 단위로 묶이고, 나중에 푸는 게 더 어렵다.

### 예외 — 관례이고 비용이 0이면 관례를 따른다 (2026-09-10 추가)

위 규칙은 **추상화**(헬퍼로 묶는 것)를 겨눈 것이다. 그런데 "아직 안 아프다"를 넓게 읽으면 **라이브러리가 정해둔 관례까지 거부하게 된다.** 그건 이득이 없다 — 관례를 따르는 것은 추측이 아니라 **남이 이미 밟은 함정을 물려받는 것**이다.

둘을 **다** 만족할 때만 관례를 따른다.

| 조건 | 무엇을 확인하나 |
|---|---|
| **관례인가** | 그 도구의 **문서·공식 예제**가 그렇게 쓰나. "내 취향"이나 "다른 데서 본 것"은 관례가 아니다 |
| **비용이 0인가** | ① 실행 비용이 없고 ② 읽는 사람이 한 단계 더 들어가지 않아도 되고 ③ 틀렸을 때 **조용히** 틀리지 않는다 |

셋 중 ③이 제일 중요하다. 비용이 0처럼 보여도 **틀렸을 때 조용히 통과하는 것**이면 비용이 0이 아니다 — 위 `위양성` 장이 막는 것과 같은 종류다.

**★ 대신 "관례라서 넣었다"고 적는다. 없는 위험을 지어내 정당화하지 않는다.** 이게 이 규칙의 진짜 요점이다.

2026-09-10 실측 — `conftest.py` 의 `suspend_global_capture(in_=True)` 에서 `in_=True` 는 이 저장소에서 **아무 일도 안 한다.** 인증번호를 터미널이 아니라 브라우저 창에 치므로 stdin 을 쓰는 코드가 한 줄도 없다. 그런데 이유를 대면서 *"안 열어두면 터미널이 응답 없이 멈춘 것처럼 보이는데 원인 찾기가 아주 어렵다"* 고 적었다. **확인해보니 안 멈춘다** — `OSError: pytest: reading from stdin while output is captured! Consider using -s.` 로 즉시 죽고, 그 메시지가 원인과 해결책을 다 알려준다.

```python
# 나쁨 — 없는 위험을 지어내 정당화한다. 다음 사람이 '못 건드리는 코드'로 알고 피해 간다
capman.suspend_global_capture(in_=True)   # 안 열면 터미널이 멈춘 것처럼 보인다

# 좋음 — 관례라고 적는다. 근거가 약하다는 것이 그대로 드러난다
# in_=True 는 stdin 도 함께 연다. 지금은 stdin 을 쓰는 코드가 없어 효과가 없지만,
# pytest 의 대화형 예제가 이렇게 쓰므로 관례를 따른다.
capman.suspend_global_capture(in_=True)
```

근거를 부풀리면 두 가지가 망가진다. 다음 사람이 **못 건드리는 코드**로 알고 피해 가고, 그 자리를 정리할 때가 와도 "위험하다니 두자"로 남는다. 관례는 관례라고만 적으면 된다 — 그게 **약한 근거**라는 걸 알아야 다음 사람이 지울지 말지를 판단할 수 있다.

## 행 식별 — 먼저 "식별이 필요한가"를 묻는다

**행을 식별해야 하는 TC는 생각보다 적다.** "N번째 행"이 아니라 **"모든 행이 조건을 만족한다"**는 전수 검사로 바꿀 수 있으면 식별 문제 자체가 사라진다 — `_assert_all_rows_have_reseller`가 이 형태고, PK가 없는 화면인데도 아무 문제가 없다.

정말 식별이 필요한 자리는 셋이다: **특정 행을 눌러 상세 열기 · 페이지네이션(1페이지 끝과 2페이지 첫 행이 같은 건인지) · 조작 전후로 같은 행 다시 찾기.** 이때는 이 순서로 내려간다.

| 순위 | 방법 | 조건 |
|---|---|---|
| 1 | PK가 담긴 속성 (`fs-table__row--10679`) | **FE가 붙여줬을 때만 쓸 수 있다.** 없으면 요청 대상이지 전제가 아니다 |
| 2 | 이미 있는 안정 속성 (`id`, `href`의 쿼리스트링, `value`) | 요청하기 전에 DOM을 먼저 열어본다 — 있는데 모르고 지나칠 수 있다 |
| 3 | **컬럼을 지정한** 정확 일치 | 행 전체가 아니라 그 컬럼만, `has_text`(부분 일치) 대신 정확 비교 |
| 4 | 인덱스 (`nth(i)`) | **같은 렌더 안에서만.** 다시 그려지면 무효 |

**표시 텍스트로 행을 찾으면 두 가지가 깨진다.**

- `has_text`는 **부분 일치**다 — `"12가3456"`으로 찾으면 `"112가34567"` 행도 걸린다
- 행 **전체**에서 찾는다 — 비고란에 그 번호가 적혀 있어도 걸린다

그래서 3번을 쓸 때는 컬럼 인덱스를 헤더 텍스트로 찾아(`_find_reseller_col_index` 패턴) **그 셀만** 비교한다. PK도 안정 속성도 없으면 `# TODO(testid):` 주석으로 남겨 FE 요청 근거로 축적한다.

## 검증 범위

- **단일 페이지**: 검증 대상 행이 여러 개면 그 페이지의 **모든 행**을 순회해 확인한다 (`_assert_all_rows_have_reseller` 패턴). 일부만 보고 통과시키지 않는다.
- **다중 페이지**: 기본은 전체 페이지를 다 도는 대신 **첫/중간/마지막 페이지를 표본**으로 뽑는다 (`_get_sample_pages`). 단, 표본으로 뽑은 각 페이지 안에서는 그 페이지의 모든 행을 검증한다 — "표본 페이지 선택"과 "선택한 페이지 안에서의 전수 확인"은 별개다.
- TC나 기획서가 "전체 결과"·"모든 페이지"를 명시적으로 검증 대상으로 지정한 경우에만 표본 없이 전체 페이지를 순회한다.
- 페이지 이동 후에는 실제로 그 페이지에 도착했는지 확인하고 넘어간다 — 이동이 조용히 실패해도(예: next 버튼 비활성) 모르고 지나칠 수 있기 때문.
- 실시간으로 갱신되는 데이터(모니터링 화면 등)는 "미리 계산해둔 페이지 번호"보다 "그 순간 실제로 도착한 곳"을 신뢰한다 (`_go_to_last_page` 참고).
- **필터·검색 결과는 한 방향만 보면 반쪽이다.** "보이는 행이 조건에 맞는가"(오검출 방지)뿐 아니라 **"조건에 맞는 게 결과에서 빠지지 않았는가"(누락 방지)**도 함께 확인한다. 뒷방향을 확인하려면 조건에 맞아야 할 목록을 화면의 다른 곳(전체 목록, 다른 탭 등)에서 미리 확보해두고 대조해야 한다.

## 위양성 — 통과하는데 검증 안 하는 테스트

**실패하는 테스트는 고치면 된다. 검증 안 하면서 통과하는 테스트는 "여긴 확인됐다"는 잘못된 안심을 준다.** 통과율로는 영원히 안 보인다 — 리포트가 초록불이니 아무도 열어보지 않는다. 원인은 거의 항상 하나다: 검증이 까다로울 때 조건을 느슨하게 만들어 초록불을 만든 것.

TC를 쓴 뒤 반드시 자문한다:
**"이 검증 대상이 망가졌다고 가정하면, 이 테스트가 빨간불이 되나?"**
답이 "아니오"면 그 TC는 없는 것만 못하다.

### 자주 걸리는 형태

| 형태 | 나쁜 코드 | 왜 통과하나 |
|---|---|---|
| 부분 문자열 | `assert "완료" in status` | `"배정완료"`·`"운영완료"`도 참 |
| 하한 없음 | `assert after < before` | `after == 0`이어도 참 |
| 빈 값 건너뛰기 | `if not v: continue` | 값이 빈 게 **버그의 증거**인데 그걸 버림 |
| 항진명제 | `assert x == sorted(x)` | 원소가 전부 같으면 오름·내림 둘 다 참 |
| 자기충족 | 탐색 조건 == 검증 조건 | 있는 것만 골라와서 있는지 확인 |
| 빈 문자열 | `assert name in title` | `name == ""`이면 항상 참 |
| 라벨만 확인 | `to_contain_text("현재 로그인 계정")` | 항상 박힌 안내 문구라 값이 비어도 참 |
| 수집만 하고 안 봄 | `found = ...` 후 `print(len(found))` | `assert`가 아예 없다 |
| **0회 루프** | `for r in rows: assert ...` | 행이 0개면 **검증이 통째로 사라짐** |

0회 루프는 이 저장소에서 실제로 걸렸다 — `_assert_all_rows_have_reseller`가 `for i in range(row_count)`만 돌고 있었다. TC-086이 1페이지에서만 `filtered_row_count > 0`을 확인하므로, 중간·마지막 표본 페이지에서 행이 0건으로 오면 검증이 통째로 사라진 채 통과했다. 지금은 루프 앞에 `assert row_count > 0`이 있다.

### 부정 검증은 방향이 반대다 — 제일 위험하다

"없어야 한다"를 볼 때는 **덜 읽을수록 통과한다.** 긍정 검증이면 덜 읽었을 때 실패해서 금방 드러나는데, 부정 검증은 목록이 채워지기 전에 읽으면 기능이 완전히 망가져도 초록불이다.

**목록으로 "없어야 한다"를 볼 때는 둘을 같이 건다** — ① 목록이 안정된 뒤 읽는다 ② 목록이 비면 실패로 본다. ②가 없으면 하한이 사라져 위 "하한 없음"과 같은 구멍이 된다.

가장 간단한 방법은 **부정 검증 앞에 긍정 검증을 두는 것**이다. `test_TC087`이 이 형태다 — `assert "리셀러" in header_texts`가 하한 역할을 해서, 헤더를 못 읽으면 `assert "파트너" not in ...`에 도달하기 전에 실패한다. `test_TC085`의 두 방향 검증도 같은 구조를 쓴다: 앞의 `expect()` 루프가 목록 렌더 완료를 보장하는 대기를 겸하고, 그 뒤 `assert actual_options`가 하한을 건 다음에야 "섞인 게 없는가"를 본다.

### assert를 쓰기 전에 "정답을 어디서 가져올지"부터 정한다

정답 출처가 없으면 쓸 수 있는 검증은 `assert x`(비어 있지 않다) 정도뿐이고, 그건 거의 항상 통과한다. 정답 출처는 대개 화면 안에 있다 — 표 컬럼 짝(`차량번호`↔`업체명`), 다른 탭의 같은 데이터, 목록 요약 vs 상세. 그것도 없으면 API 응답이다.

**라벨과 값을 구분한다.** 화면에 항상 박혀 있는 안내 문구를 `to_contain_text`로 확인하면 정작 그 옆의 값이 비어도 통과한다. 값 전용 셀렉터가 없으면 정규식으로 라벨 앞뒤의 값을 뽑아 검사한다.

### 검증 대상을 탐색 조건에 넣지 않는다

탐색 조건은 **전제**(상태·유형)까지만 담고, 검증할 것은 반드시 본문 `assert`로 남긴다. 탐색 조건에 넣으면 "있는 것만 골라와서 있는지 확인"이 되어 실패가 불가능해지고, 회귀가 나도 **"못 찾음 → skip"**으로 노란불에 묻힌다 (아래 `pytest.skip` 사용 기준 참고).

### 같은 것을 읽는 방법은 하나여야 한다

셀렉터 이름이 멀쩡해도 **"이걸 어떻게 읽어야 하는가"라는 지식이 한 곳에 없으면** TC마다 다른 방법을 골라 쓰게 되고, 그중 하나가 위양성이 되어도 이름은 멀쩡해서 아무도 눈치채지 못한다. 실제로 겪음 — `_get_real_row_count()`가 "조회 결과가 없습니다" 안내 행(colspan)을 걸러주는데 `_assert_all_rows_have_reseller`는 `rows.count()`로 직접 세고 있었다. 헬퍼를 고를 때는 이름이 아니라 **구현을 열어서** 확인한다.

### 자가 점검은 뚫린다

**이 규칙을 쓴 사람이 스스로 점검하면 못 잡는다** — 자기가 그렇게 쓴 이유를 알고 있으니 다시 봐도 같은 결론이 나온다. 코드를 안 쓴 사람에게 리뷰를 부탁하거나, 서브에이전트에게 "이 파일에서 통과하는데 검증 안 하는 곳을 찾아라"고 시키거나, 최소한 하루 지나서 다시 본다. **초록불은 통과의 근거가 아니다.**

## `pytest.skip` 사용 기준

**"검증할 데이터가 없다"와 "데이터가 없어야 정상이다"를 구분한다.**

- TC가 특정 데이터의 존재를 전제로 하는데 그 데이터가 없어서 검증 자체가 불가능한 경우에만 `pytest.skip`을 쓴다 (예: 그 탭에 조회되는 행이 아예 0건이라 컬럼 인덱스조차 계산할 수 없는 경우).
- **필터·조건을 적용한 "결과"가 0건인 것은 skip 대상이 아니다.** 이는 실패일 수 있는 상태이므로 `assert`로 명시적으로 실패시킨다 (`assert filtered_row_count > 0` 패턴). 결과 0건을 skip으로 처리하면 실패가 PASS처럼 보여 회귀를 놓친다.
- "데이터가 없어야 정상"인 TC(빈 상태 검증)라면 skip이 아니라 그 빈 상태 자체를 검증 대상으로 삼는다.
- 환경 자체의 이유로 테스트 수행이 기술적으로 불가능한 경우(예: 해당 환경에 기능 자체가 배포되지 않음)에만 위 원칙과 별개로 skip을 허용한다.
- **skip 사유가 "데이터가 없다"일 때는 표본을 충분히 본 뒤에 판단한다.** 1페이지만 보고 없다고 단정하지 않는다 — 페이지를 넓히거나 다른 탭·조건까지 확인한 뒤 skip 여부를 정한다.
- **"데이터가 없다"는 skip 메시지가 환경 장애를 가릴 수 있다.** 세션이 서버에서 끊기거나 페이지 로딩이 실패해도 조회 결과가 빈 것처럼 보인다. skip이 나오면 같은 조건으로 화면을 다시 열어 **실제로 데이터가 없는 건지, 로그인·로딩이 실패한 건지**부터 구분한다.

## 실행 전제와 실패 구분

- 로그인 실패·서버 접속 불가처럼 **테스트 대상 기능에 도달하기도 전에** 막히는 경우는 "환경 문제"로 구분하고, 실제 화면 동작이 기대와 다른 "기능 검증 실패"와 섞지 않는다.
- fixture(`logged_in_page` 등) 단계에서 실패하면 pytest가 이미 `ERROR`로 표시해 일반 실패(`FAIL`)와 구분된다 — 이 구분을 그대로 살린다. 테스트 본문 안에서 로그인·페이지 이동 실패를 `try/except`로 삼켜 assert 하나로 뭉개지 않는다.
- **연속으로 `E`(error)가 쏟아지면 기능 버그보다 환경 문제(세션 충돌 등)를 먼저 의심한다** (위 "pytest를 동시에 두 개 이상 돌리지 않는다" 참고). 실패(`F`) 한 건 뒤로 `E`가 줄줄이 이어지는 패턴이 전형적인 신호다.
- `pytest.skip`을 "데이터 없음"으로 처리하기 전에도 같은 원칙이 적용된다 — 세션 끊김·로딩 실패로 결과가 비어 보이는 것과 실제로 데이터가 없는 것을 구분한다 (아래 `pytest.skip 사용 기준` 참고). 이 섹션과 그 섹션은 "환경 문제와 기능 결과를 섞지 않는다"는 같은 원칙의 두 적용 사례다.

## 데이터를 바꾸는 동작 — 실행 금지

**[저장]·[제출]·[신청 완료]·[취소처리]·[삭제]처럼 서버 데이터를 바꾸는 버튼은 누르지 않는다.** 검증은 **"필드가 노출되는가 / 버튼이 활성화됐는가"**까지만 한다. 아래 `변경성(mutating) 테스트 준비`의 옵트인 장치가 갖춰지고, 그 TC로 명시적으로 지시받기 전까지 예외는 없다.

- **"어차피 유효성 검사에서 막히겠지"를 근거로 누르지 않는다.** 막아주는 그것이 바로 검증 대상이라, 회귀가 나 있으면 그대로 데이터가 생긴다. 다른 저장소에서 실제로 이렇게 dev에 서비스 신청 건이 생성됐다.
- **위험한 버튼은 화면 객체에 property로 두되, 그것을 누르는 메서드는 만들지 않는다.** 존재·활성 확인은 해야 하므로 요소 자체는 필요하지만, `submit()` 같은 메서드가 있으면 다음 사람이 쓴다 (`pages/field_service_page.py`의 `NewServicePopup`이 이 형태 — `submit_button`은 있고 `submit()`은 없다).
- **위험한 요소에는 ★ 주석을 단다.** 화면 객체·테스트 파일 상단·해당 TC의 docstring 세 곳에 적는다. 한 곳에만 적으면 다른 경로로 들어온 사람이 못 본다.
- **팝업 안에서만 끝나는 동작은 안전하다** ([목록에 추가] 등). 서버에 요청이 나가는지로 구분하고, 애매하면 누르지 않는다.
- **이 규칙은 코드 리뷰로 못 잡는다** — 눌러봐야 막히는지 알 수 있다. 그래서 규칙으로 막는다.

**★ 지금 이 저장소에 이미 예외가 하나 있다.** 2026-09-09 확인 —
`test_vehicle_company_transfer.py` 의 TC-059~066 은 차량 수정 모달에서 **[수정] 저장을 실제로
실행한다**(`_click_save_and_confirm`). 이관이 허용되는 조합(059 · 061 · 063 · 065)은 dev
데이터를 실제로 바꾸고, 차단되는 조합(060 · 062 · 064 · 066)도 **저장을 시도하는 것까지는 같다.**
이관 전용 차량 4대(`900용1001~1004`)만 쓰고 fixture teardown 이 원래 업체로 되돌리도록 만들어져 있다 —
경위는 `docs/notes/code-notes/차량업체변경-테스트-노트.md` 참고.

**"이 저장소 테스트는 전부 읽기 전용" 이라고 알고 시작하면 안 된다.** 새 화면을 맡을 때
이 문장 하나로 판단하지 말고, 그 파일에 `save`·`submit` 계열 클릭이 있는지 먼저 본다.

## 변경성(mutating) 테스트 준비 — 최소 규칙

**★ 이 장은 "앞으로의 대비" 가 아니라 "이미 밀린 숙제" 였다.** 2026-09-09 확인 —
위 장에 적은 대로 TC-059~066 이 이미 저장을 실행하고 있었는데 아무 장치도 없었다.
**①(옵트인 게이트)·②(원복을 teardown 으로)·③(마커) 을 그날 전부 했다.**
지금 `pytest -v` 를 그냥 돌리면 그 8건은 **게이트가 skip 한다**(61 실행 + 8 skip).
돌리려면 명령에 `--allow-mutating` 을 붙인다.

**①②③ 에 더해 접속처 화이트리스트까지 그날 넣었다** — ① 참고. 네 겹이 다 있다.

그 전까지 이 자리에는 "지금 있는 테스트는 전부 읽기 전용이라 아직 위험이 없다" 고 적혀 있었다.
그 문장이 틀린 채로 남아 있어서 이 저장소를 안전하다고 믿고 전체 실행을 걸 수 있었다 —
**규칙 문서가 틀리면 그 문서로 하는 판단이 통째로 샌다.**

세부 구현(URL 판별 방식, 접두어 형식 등)은 실측 시 화면·환경을 직접 보고 확정하며,
확정되면 이 섹션과 관련 code-notes를 함께 갱신한다.

**① 실행 환경 안전장치 — 2026-09-09 에 옵트인 게이트를 넣었다**
- mutating 테스트는 명시적 옵트인 없이는 실행되지 않는다 — `conftest.py` 의 `_mutating_gate`(autouse fixture)가 setup 단계에서 스스로 skip 한다.
- **켜는 방법은 둘이고, 차이는 "얼마나 오래 사는가" 다. 플래그를 쓴다.**

  | 방법 | 사는 범위 | 쓸 자리 |
  |---|---|---|
  | `pytest --allow-mutating` | **그 명령 한 줄** | 평소 (권장) |
  | 셸 `$env:ALLOW_MUTATING_TESTS="true"` | 그 터미널 탭 | 여러 번 반복해서 돌릴 때 |
  | `.env` 에 `ALLOW_MUTATING_TESTS=true` | **지울 때까지** | CI 등 매번 붙이기 어려운 자리 |

  뒤로 갈수록 **끄는 것을 잊을 위험**이 커진다. 켜둔 채 잊으면 게이트가 없는 것과 같다. `load_dotenv()` 는 `override=False` 라 셸 값이 `.env` 를 이긴다(2026-09-09 실측).
- **플래그 오타는 조용히 통과하지 않는다** — `--allow-mutatng` 은 pytest 가 `unrecognized arguments` 로 즉시 끊는다. `.env` 쪽 오타는 그냥 "안 켜짐" 이 되어 skip 으로 나타난다. 둘 다 안전한 방향으로 틀린다.
- 확인(2026-09-09): 플래그 없이 `pytest -m mutating -v` → **8 skipped**. `--allow-mutating` 을 붙이면 통과한다.
- **★ 2026-09-10 정정 — 위 확인에 달려 있던 괄호가 틀렸다.** *"브라우저도 로그인도 일어나지 않는다 — 게이트가 `logged_in_page` 보다 먼저 돌기 때문"* 이라고 적어뒀는데, **근거도 결론도 틀렸다.**

  | | 적혀 있던 것 | 실제 |
  |---|---|---|
  | 근거 | 이 fixture 는 **의존이 없어서** 먼저 돈다 | pytest 는 의존보다 **스코프를 먼저** 본다. `auth_state`(session)가 `_mutating_gate`(function)보다 **항상 먼저**다 |
  | 결론 | 차단될 때 **브라우저가 아예 안 뜬다** | 안 만들어지는 건 `logged_in_page` 뿐. **`auth_state` 는 이미 돈 뒤**라 브라우저가 뜨고, 세션이 죽어 있으면 2FA 창까지 뜬다 |
  | 그때 왜 맞아 보였나 | — | 09-09 엔 `auth.json` 이 신선해서 `auth_state` 가 아무 일도 안 했다. 게이트가 먼저 돈 게 아니라 **`auth_state` 가 조용했던 것** |

  근거 쪽이 더 위험하다 — **"의존이 없으면 먼저 돈다"는 다른 곳에서도 틀리게 쓰일 지식**이다. 순서 규칙은 ① 스코프가 큰 것 ② 같은 스코프 안에서 autouse ③ 그다음 의존 순이다.

  증거: 2026-09-10 10:20 실행에서 TC-059 가 `skipped` 가 아니라 **로그인 실패로 `failed`** 났다 — 게이트가 손쓰기 전에 로그인이 먼저 시도됐다는 뜻이다. 같은 구조를 따로 만들어 재현해도 `auth_state` 가 1번, `_mutating_gate` 가 2번으로 찍힌다.

  **안전에는 영향이 없다.** 순서가 어떻든 게이트는 항상 발동하고 데이터를 바꾸는 코드까지는 안 간다. 순서가 바꾸는 건 "skip 하기 전에 로그인을 한 번 하느냐" 뿐이고, 그게 **낭비가 되는 명령은 `pytest -m mutating -v`(플래그 없이) 하나뿐**이다. 나머지 경로는 읽기 전용 TC 가 섞여 있어 어차피 로그인이 필요하다.

  | 명령 | 순서가 바뀌면 달라지나 |
  |---|---|
  | `pytest -v` (전체) | 읽기 전용 61건이 어차피 로그인을 쓴다 — 차이 없음 |
  | `pytest test_vehicle_company_transfer.py` | 그 파일의 TC-057·058 이 mutating 이 아니다 — 차이 없음 |
  | `pytest -m mutating --allow-mutating -v` | 8건이 실제로 돈다 — 차이 없음 |
  | `pytest -m mutating -v` | **여기만** 8건 전부 skip 인데 브라우저가 뜬다 |

  **언제 고칠 값어치가 생기나** — `-m mutating` 만 따로 돌리는 일이 잦아지면 판정을 `pytest_runtest_setup` 훅으로 옮긴다. 그 자리는 **모든 fixture 보다 먼저** 돌아서 위 문장이 그제서야 사실이 된다. 지금 안 옮기는 이유는 그 한 명령을 위해 **안전장치를 건드리는 것**이라 이득 대비 위험이 안 맞기 때문이다.
- **마커와 게이트는 하는 일이 다르다.** 마커는 "고를 수 있게", 게이트는 "모르고 돌리는 것을 막게". `-m "not mutating"` 이 안 붙는 경로(`pytest -v` · `pytest --lf` · 파일 지정 실행 · PyCharm 실행 버튼 · 새로 온 사람)가 많아서 마커만으로는 못 막았다.
- `.env` 는 `.gitignore` 에 있어 커밋되지 않는다 — 그래서 남의 컴퓨터·CI 에는 값이 없고 **기본이 안전한 쪽**으로 유지된다. 이게 옵트인의 요점이다.
- **접속처 화이트리스트 — 허용을 켰어도 아는 곳이 아니면 막는다** (2026-09-09 추가). `conftest.py` 의 `MUTATING_ALLOWED_HOSTS` 에 없는 호스트면 `pytest.fail` 로 끊는다.
  - **왜 필요한가** — 옵트인은 "모르고 도는 것" 은 막지만 "알고 돌렸는데 대상이 운영인 것" 은 못 막는다. ① 운영 화면을 볼 일이 있어 `.env` 의 `STAFF_URL` 을 운영으로 바꿔 둔다 → ② 며칠 뒤 이관 TC 를 돌리려고 `--allow-mutating` 을 붙인다 → ③ 실제 고객 데이터가 바뀐다. **각각은 멀쩡한 판단인데 조합이 사고다.**
  - **블랙리스트가 아니라 화이트리스트인 이유** — "운영이면 막는다" 로 만들려면 운영 도메인이 어떻게 생겼는지 알아야 하는데 아직 모른다. 추측하면 너무 좁아 **안 막히거나**(`"prod" in url` 인데 운영이 그렇게 안 생김) 너무 넓어 dev 까지 막힌다. 뒤집어 두면 **운영 주소를 몰라도 되고**, 새 환경이 생겨도 처음 보는 주소는 자동으로 막힌다. dev 주소가 바뀌면 "안 돌아감" 쪽으로 틀리므로 안전하다.
  - **검사 순서는 "허용 → 접속처" 다.** 접속처를 먼저 보면 허용을 켜지도 않은 평범한 `pytest -v` 가 낯선 환경에서 8건 전부 빨간불이 된다 — 어차피 안 돌 테스트인데 시끄럽고, 그러면 사람이 가드를 약하게 만들려 든다. **정말 돌 뻔한 순간에만** 끊는다. 안전성은 같다.
  - **`skip` 이 아니라 `fail`** — skip 은 "조건이 안 맞아 안 했다" 는 정상 상태인데, 이건 사고 직전이라 빨간불이 나야 알아챈다. fixture 안에서 나므로 리포트에는 setup 단계 `ERROR` 로 찍힌다.
  - 확인(2026-09-09) — dev+플래그없음 `skipped` / dev+플래그 `passed` / 낯선 주소+플래그없음 `skipped`(조용함) / 낯선 주소+플래그 **`ERROR`**. 읽기 전용 TC 는 네 경우 모두 영향 없음.

**② 테스트 데이터 정리(cleanup) 기준**
- mutating 테스트가 생성하는 데이터에는 자동 생성분임을 구분할 수 있는 접두어를 붙인다(예: `QA_AUTO_` + 타임스탬프). 정확한 형식은 화면의 이름/코드 필드 제약(길이, 허용 문자)을 실측한 뒤 정한다.
- 테스트가 만든 데이터는 그 테스트 안에서 삭제·원상복구까지 책임진다(setup에서 만들면 teardown에서 지운다). **정리(cleanup)에 실패하면 그 자체를 테스트 실패로 본다** — 예외를 삼켜 조용히 넘어가지 않는다.
- 구조상 삭제가 불가능한 데이터(이력성 로그 등)는 mutating 테스트 대상에서 제외하거나, 남는 데이터를 code-notes에 목록으로 남겨 추적한다.
- **★ 2026-09-09 에 원복을 fixture teardown 으로 옮겼다.** 전에는 테스트 본문 마지막 줄이라 중간에서 실패하면 그 줄에 도달하지 못해 차량이 바뀐 채로 남았다. 지금은 `test_vehicle_company_transfer.py` 의 `company_guard` fixture 가 맡는다 — 원래 값을 읽은 직후에 등록해 두면, 통과든 assert 실패든 예상 못 한 예외든 **teardown 이 반드시 되돌린다.**
  - **차단(blocked) TC 4건도 등록한다.** 정상이라면 데이터가 안 바뀌지만, 막아주는 그것이 검증 대상이라 정책이 회귀해 있으면 저장이 실제로 통과한다. 전에는 그 경우 원복할 곳이 아예 없었다.
  - 원복은 **현재 값을 먼저 읽고 이미 원래 값이면 아무것도 하지 않는다.** 되돌릴 게 없는데 [수정]을 누르는 것 자체가 규칙이 막는 동작이다.
  - 원복 실패는 **삼키지 않는다** — teardown 에서 그대로 터져 pytest 가 `ERROR` 로 표시한다. 검증 실패(`FAILED`, call 단계)와 단계가 달라서 서로 가리지 않는다.
  - **★ "`ERROR` 가 없으니 원복됐다" 는 틀린 추론이다.** 2026-09-09 에 이걸로 한 번 속았다 — `_restore_company` 는 `현재값 == 원래값` 이면 **조용히 `return`** 하므로, 예외가 없다는 것은 "teardown 이 안 터졌다" 일 뿐 "되돌렸다" 가 아니다. 실제로는 4대 중 3대가 어긋난 채였다. **원복 확인은 데이터를 직접 읽어서** 한다.
  - **★ 목적지가 하드코딩이고 원래 값을 화면에서 읽는 구조는, 차량이 이미 목적지에 있으면 통째로 헛돈다.** `원래값 == 목적지` 가 되어 이관이 제자리 저장이 되고, 검증도 teardown 도 전부 통과한다 — **초록불인데 정책을 하나도 안 본다**(`위양성` 장의 자기충족). 그래서 이관 직전에 `_assert_not_already_there()` 로 전제를 확인하고, 어긋나면 skip 이 아니라 **fail** 로 끊는다.

**③ 테스트 태그/실행 범위 — 여기까지는 2026-09-09 에 했다**
- `pytest.ini` 에 `mutating` 마커를 등록하고, `test_vehicle_company_transfer.py` 의 **TC-059~066 여덟 건**에 `@pytest.mark.mutating` 을 붙였다. 저장에 성공하는 넷뿐 아니라 차단되는 넷도 **저장을 누르는 것까지는 같으므로** 함께 붙였다.
- 확인: `pytest -m "not mutating" --collect-only -q` → **61/69** · `-m mutating` → **8/69**.
- **`-m` 과 `--allow-mutating` 은 하는 일이 다르다.** `-m` 은 **고르고**(선택), 플래그는 **돌려도 되는지**(허용)를 정한다. 별개의 단계라 조합이 넷이다 — `--allow-mutating` 은 "mutating 만 돌려라" 가 아니다.

  | 명령 | 실제로 돌아가는 것 |
  |---|---|
  | `pytest -v` | **61 passed, 8 skipped** — 읽기 전용만 |
  | `pytest --allow-mutating -v` | **69 전부** — 읽기 전용 61 + 저장하는 8이 같이 |
  | `pytest -m mutating -v` | **8 skipped, 61 deselected** — 아무것도 안 돎 |
  | `pytest -m mutating --allow-mutating -v` | **8만** — 저장하는 것만 |

  `deselected`(`-m` 이 안 골랐다)와 `skipped`(골랐는데 게이트가 막았다)는 다른 말이다. 그래서 `-m mutating` 만 붙이면 **하나도 안 돈다** — 저장하는 8건을 실제로 돌리려면 둘 다 붙여야 한다. skip 이 리포트에 뜨는 것도 싫으면 `-m "not mutating"` 을 쓴다(이제 안전용이 아니라 리포트 정리용이다).
- **마커는 "고를 수 있게" 만든 것이지 "모르고 돌리는 것을 막는" 장치가 아니다.** 그쪽은 ①의 옵트인 게이트가 맡는다(2026-09-09 에 넣었다). 그래서 이제 `pytest -v` 는 그 8건을 skip 한다 — `-m "not mutating"` 은 "리포트에서 아예 빼고 싶을 때" 쓰는 것이지 안전을 위해 필요한 것이 아니다.
- **등록은 사용 조건이 아니다.** `@pytest.mark.아무이름` 은 등록 없이도 동작한다(`conftest` 가 붙이는 `tc_260907` 이 그 증거 — 등록된 적 없는데 `-m tc_260907` 로 15건이 골라진다). `pytest.ini` 에 적는 이유는 ① 경고를 없애고 ② 나중에 `--strict-markers` 를 켤 수 있게 하기 위함이다.
- **`--strict-markers` 는 아직 안 켰다.** 켜면 미등록 마커가 경고가 아니라 에러가 되는데, `conftest` 훅이 붙이는 `tc_*` 는 배포일마다 새로 생겨 미리 등록할 수 없어 **INTERNALERROR 로 죽는다**(2026-09-09 실측). 켜려면 그 훅에서 `add_marker` 앞에 `item.config.addinivalue_line("markers", f"tc_{date}: …")` 를 같이 넣어야 하고, 그 조합이면 통과하는 것까지 확인했다. 지금 안 켠 이유는 손으로 붙이는 마커가 `mutating` 하나뿐이라 오타가 날 자리가 좁기 때문이다 — **`mutating` 마커가 늘어나면 켜는 쪽이 맞다.** 그 오타는 테스트를 빨갛게 만드는 게 아니라 **`-m "not mutating"` 필터를 빠져나가게** 만들어서, 위양성과 같은 방향으로 조용히 틀린다.
- 그 대신 `tc_*` 경고만 `filterwarnings` 로 껐다(`ignore:Unknown pytest.mark.tc_:…`). 메시지 앞부분 매칭이라 `mutatng` 같은 진짜 오타는 여전히 경고가 뜬다.
- `smoke`·`readonly` 같은 추가 marker는 지금 쓸 곳이 확인된 게 없어 만들지 않는다 — 필요해지면 그때 추가한다(안 쓰는 marker를 미리 만들면 뭘 기준으로 태깅해야 하는지 기준 없이 방치된다).

## 테스트 독립성

- 각 테스트는 다른 테스트의 실행 결과·순서에 의존하지 않는다. TC A가 만든 데이터를 TC B가 전제로 쓰지 않는다.
- 테스트가 데이터를 생성·수정·삭제하면, 그 테스트 안에서 필요한 걸 준비하고 다른 테스트에 영향이 남지 않게 한다.
- **이 원칙은 이미 적용 중이다.** 대부분은 읽기 전용이지만 `test_vehicle_company_transfer.py` 의 TC-059~066 이 데이터를 바꾼다. 그쪽은 이관 전용 차량 4대(`900용1001~1004`)를 다른 TC(TC-051~058)와 겹치지 않게 나눠 쓰고, **`company_guard` fixture 의 teardown 이** 원래 업체로 되돌려 반복 실행이 가능하게 하는 방식으로 독립성을 지킨다 (2026-09-09 부터 — 그 전에는 본문 마지막 줄이라 중간 실패 시 상태가 남았다).

## 테스트 데이터

- 실제 환경에 이미 있는 데이터가 있으면 임의의 값을 새로 만들지 않고 그것을 우선 사용한다 (예: `RESELLER_OPTIONS`처럼 실제 화면에서 확인한 값).
- TC/기획서에 없는 값을 "정상 데이터"로 임의로 간주하지 않는다 — 예를 들어 등록 폼에 넣을 임의의 이름·번호 같은 값이 필요하면, 그 조건이 TC나 노트에 근거하고 있는지 먼저 확인한다.

## 테스트 코드 컨벤션

- **함수명: `test_<영문 설명>` — TC ID는 넣지 않는다.** (예: `test_monitor_reseller_filter_query_result`)
  - (2026-09-04 변경) 기존 TC(`TC-001`~`TC-1xx`대)는 `test_TC<번호>_<영문 설명>`로 이미 작성돼 있고 소급 변경하지 않는다. **새 TC부터**는 함수명에서 번호를 뺀다 — 새 TC ID 형식(`TC-YYMMDD-001`, 아래 "TC ID 형식" 참고)은 하이픈이 있어 파이썬 식별자로 쓸 수 없고, 그대로 욱여넣으면(`test_TC260904001_...`) 읽기가 나빠진다.
  - ID와 함수의 연결은 함수명이 아니라 `@allure.label("testcase", "TC-260904-001")` 데코레이터가 **유일한 정본**이다. 실행 시 필요한 ID 기반 필터링(`pytest -m tc_260904`)은 이 값에서 conftest 훅이 자동으로 마커를 만들어 처리한다 — 아래 "TC ID 형식과 배포 단위 실행" 참고.
- 여러 탭/화면에 동일 로직을 반복 검증할 땐 `@pytest.mark.parametrize`로 묶는다. 한글 파라미터는 pytest가 테스트 ID를 `\uXXXX`로 이스케이프하므로 `ids=`로 영문 id를 따로 지정한다.
- **테스트 제목은 `@allure.title` 한 곳에만 둔다.** `TC-XXX | 한 줄 요약` 형식으로 쓰고, docstring 첫 줄에 같은 요약을 반복하지 않는다 — 두 곳에 적으면 나중에 한쪽만 고쳐져 서로 어긋난다(실제로 겪음).
- 모든 테스트 함수 docstring은 GIVEN/WHEN/THEN 형식으로 작성한다. 요약은 빼고 상세만 담는다:
  ```python
  @allure.label("testcase", "TC-260904-001")
  @allure.title("TC-260904-001 | 한 줄 요약")
  def test_...(logged_in_page: Page) -> None:
      """
      GIVEN  사전 조건
      WHEN   수행하는 동작
      THEN   기대 결과
      """
  ```
  GIVEN/WHEN/THEN 외에 덧붙일 배경(TC 문서 비고, 데이터 전제 등)이 있으면 그 아래에 이어서 쓴다.
- `assert` 실패 메시지는 `"[FAIL] ..."`로 시작해 무엇이 왜 잘못됐는지 값과 함께 남긴다.
- 화면 특유의 데이터(예: dev 환경 리셀러 목록)는 TC 문서와 다를 수 있음을 주석으로 남기고, 실제 화면 확인 결과를 기준으로 삼는다.
- 셀렉터는 화면 객체(`pages/`)에, 반복되는 UI 조작·검증 로직은 helper로 분리한다 — **무엇을 뺄지 판단하는 기준은 위 `Page Object` 장의 표를 따른다**(반복 횟수만으로 정하지 않는다). helper로 감싸더라도 테스트 함수만 보면 무엇을 검증하는지 드러나야 한다 — 유지보수 편의를 위한 추상화가 TC 의도를 가리면 안 된다.
- 함수 시그니처에는 타입 어노테이션을 붙인다 (`page: Page`, `-> Locator`, `-> None`). PyCharm 자동완성이 정확해지고, 매개변수에 뭘 넣어야 하는지가 코드만 보고 드러난다.

## Allure 리포트 컨벤션

적용 배경과 상세는 `docs/notes/Allure-적용-노트.md` 참고. 새 테스트를 쓸 때 지킬 것만 여기 남긴다.

- **`@allure.title`** — 모든 테스트에 붙인다. 파라미터가 있으면 `{tab_name}`처럼 값을 끼워 넣어 리포트에서 케이스가 구분되게 한다.
- **`@allure.step`** — UI를 조작하는 헬퍼(진입·선택·이동·저장 등)에 붙인다. 실패했을 때 어느 단계에서 깨졌는지 리포트에서 바로 보인다. 다만 **한 줄짜리 단순 조회 헬퍼까지 다 붙이지는 않는다** — 단계가 3~5개로 나뉘는 흐름에만 의미가 있다.
  - step 문구에 문자열 파라미터를 넣을 땐 **따옴표를 직접 쓰지 않는다.** Allure가 자체적으로 붙여줘서 `''값''`처럼 두 번 찍힌다.
- **`pytestmark = allure.feature(...)`** — 파일 맨 위에 한 줄. 값은 그 파일 상단의 GNB 경로 주석과 동일하게 맞춘다(분류 체계를 새로 만들지 않는다). 코드에는 관례대로 `"화면명  ·  test_xxx.py"`처럼 파일명까지 적어도 되는데, `postprocess_allure_results.py`가 리포트에서는 그 파일명 부분을 잘라내고 화면명만 남긴다 — 아래 "Labels 정리" 참고.
- **첨부(스크린샷·콘솔 로그)는 실패한 테스트에만** 남긴다(`conftest.py`가 자동 처리). 통과한 것까지 붙이면 리포트만 무거워진다.
- **`@allure.label("testcase", "TC-XXX")`** — 모든 테스트에 붙인다. TC 번호가 리포트 Labels에 텍스트로 남아 `@allure.title`·함수명과 세 곳이 어긋나지 않았는지 대조하기 쉬워진다.
  > **`@allure.testcase(...)`를 쓰지 않는 이유**: 이름 때문에 순수 라벨처럼 보이지만 실제로는 `allure.link(url, link_type="tms")`의 별칭이라 **첫 인자를 URL로 취급한다.** `@allure.testcase("TC-043")`을 그대로 쓰면 리포트에 `TC-043`이라는 문자열이 그대로 클릭 가능한 링크로 뜨는데, 실제 URL이 아니라서 눌러도 깨진 페이지로 간다 (직접 실행해 결과 JSON을 까본 뒤 발견 — `links: [{"type": "tms", "url": "TC-043", ...}]`으로 찍혔다). TestRail 같은 도구를 도입해 `--allure-link-pattern`으로 실제 URL을 만들 수 있게 되면 그때 `@allure.testcase`로 바꾼다.
- `@allure.severity`는 **쓰지 않는다** — TC 문서에 심각도 컬럼이 없다. 근거가 생기면 그때 붙인다.

### TC ID 형식과 배포 단위 실행 (2026-09-04 추가)

TC 엑셀 쪽 규칙은 `docs/tc-generation/AI_TC_초안_작성_지침_통합본.md` §14.1 참고. 여기는 그 ID를 자동화 코드에서 다루는 법만 담는다.

- **새 TC 파일부터 TC ID에 배포(예정)일자 접두어가 붙는다** — `TC-YYMMDD-001` (예: `TC-260904-001`). 파일마다 001부터 새로 시작하므로, 이전처럼 전역에서 다음 빈 번호를 찾을 필요가 없다.
- **기존 TC(`TC-001`~`TC-1xx`대)는 그대로 둔다.** 소급 변경 대상이 아니다 — 새 형식과 옛 형식이 코드 안에 섞여 있어도 문제없다.
- **배포일이 밀리면 배포 전까지만 엑셀·코드의 ID를 함께 치환한다.** 배포가 이미 나간 뒤에는 바꾸지 않는다 — 슬랙 공유·리포트가 이미 그 ID를 가리키고 있기 때문이다.
- **`conftest.py`의 `pytest_collection_modifyitems` 훅이 `@allure.label("testcase", ...)` 값에서 `tc_<날짜>`·`tc_<날짜>_<순번>` 마커를 자동으로 만든다.** 데코레이터가 정본이라 마커가 따로 어긋날 일이 없다.
  ```bash
  pytest -m tc_260904          # 이 배포(엑셀 파일) 분만 실행 — 여러 파일에 흩어져 있어도 한 번에
  pytest -m tc_260904_001      # TC 하나만
  ```
- 이 마커들은 날짜마다 새로 생겨서 `pytest.ini`에 미리 등록해두지 않는다 — 지금 이 프로젝트엔 `--strict-markers`가 켜져 있지 않아 미등록 마커를 써도 에러가 나지 않는다. 나중에 `mutating` 마커(위 "변경성 테스트 준비" 참고)를 등록하면서 `--strict-markers`를 켜게 되면, 이 훅이 만드는 마커들도 함께 예외 처리해야 한다.

### Labels 정리 — feature·testClass·testMethod를 분리해서 보여주기 (2026-09-03)

**문제**: TC 상세 화면의 Labels 탭에서 `feature` 값이 `"로그인 · 권한  ·  test_login.py"`처럼
화면명과 파일명이 한 덩어리로 나와서, 코드를 모르는 사람이 보기에 뭐가 뭔지 구분이 안 됐다.

**해결**: `postprocess_allure_results.py`가 pytest 실행 직후 결과 JSON을 열어 두 가지를 한다.
1. `fullName`(예: `test_login#test_TC114_...`)을 쪼개 **`testClass`**(파일명)·**`testMethod`**(함수명)
   라벨을 새로 추가한다
2. **`feature`** 라벨 값에서 `" · xxx.py"` 부분을 잘라내 화면명만 남긴다(`_clean_feature_label`)

그 결과 Labels 탭에 세 라벨이 겹치지 않고 각자 뜻대로 분리되어 뜬다.

```
feature:    로그인 · 권한
testClass:  test_login.py
testMethod: test_TC114_login_success_and_refresh_session
```

**반드시 `allure generate` 전에 후처리를 실행해야 한다** — 후처리는 결과 JSON 파일을 직접
고치는 것이라, 이미 HTML로 생성된 리포트에는 적용되지 않는다. 순서: `pytest` → `postprocess_allure_results.py` → `allure generate`.

## QA-친화적 커스텀 리포트 (부가 산출물, 2026-09-03 추가)

공식 Allure 리포트(위 Labels 정리를 거친 것)가 기본 산출물이다. 그 위에, 코드를 모르는
사람도 파일 하나만 열면 결과를 훑을 수 있도록 `tools/generate_qa_report.py`가 별도의
자체완결 HTML(`qa-report.html`)을 추가로 만든다 — 서버 없이 더블클릭으로 바로 열린다.

### 생성 순서 (공식이 먼저, 커스텀은 그 다음)

PyCharm 터미널(PowerShell) 기준:

```powershell
# 1. 테스트 실행
pytest -v --alluredir=allure-results

# 2. 후처리 (Labels 정리 — 위 섹션 참고)
python tools/postprocess_allure_results.py allure-results

# 3. 공식 Allure 리포트 (기본 산출물)
Remove-Item -Recurse -Force allure-report -ErrorAction SilentlyContinue
allure generate allure-results --output allure-report
allure open allure-report

# 4. QA 커스텀 리포트 (추가 산출물)
python tools/generate_qa_report.py allure-results -o qa-report.html
```

한 줄 실행 스크립트(`run_tests_and_report.bat`/`.sh`)도 이 순서(공식→커스텀)를 따른다 —
커스텀 리포트 생성이 실패해도 공식 리포트는 이미 나와 있도록 하기 위함이다.

### 두 리포트의 역할 분담

| 파일 | 대상 | 특징 |
|---|---|---|
| `allure-report/index.html` | 기본 — 개발자·상세 진단 | 스크린샷·콘솔 로그·단계별 실행 기록, `allure open`으로 열어야 함(서버 필요) |
| `qa-report.html` | 추가 — QA 빠른 확인 | feature/TC ID/파일명/함수명/상태/실행시간을 표로, 탭 4개(요약·화면별·상태별·전체), 더블클릭으로 바로 열림 |

상세 사용법은 `docs/QA-리포트-생성-가이드.md` 참고.

> **Allure 3 CLI 문법 주의**: 예전(Java 기반) Allure의 `--single-file`·`--clean` 옵션은
> Allure 3(Node 기반)에는 없다. `allure generate <results> --output <dir>`만 쓰고, 재생성 전
> 기존 출력 폴더는 직접 지운다 — PowerShell: `Remove-Item -Recurse -Force allure-report`,
> bash: `rm -rf allure-report`. 생성된 `index.html`을 파일 탐색기에서 바로 더블클릭하면
> 정상 표시가 안 되므로 **`allure open <dir>`로 열어야 한다**(로컬 서버로 서빙).

### PyCharm 터미널에서 `pytest` 명령이 안 먹을 때

**"인터프리터 설정"과 "터미널 활성화"는 다른 것이다.** 프로젝트 인터프리터를 `.venv`로
지정해두는 건 한 번만 하면 유지되는 설정이지만, 그 상태가 터미널 탭마다 자동으로
"활성화"(PATH가 `.venv/Scripts`를 가리키게 되는 것)로 이어지는지는 별개 문제다.
PyCharm이 새 터미널 탭을 열 때 activate 스크립트를 자동 실행해주는 게 기본값이긴 하지만,
`Settings → Tools → Terminal → "Activate virtualenv"` 옵션이나 셸 종류(cmd/PowerShell/Git Bash)에
따라 안 될 수 있다. 즉 **"프로젝트를 한 번 설정해뒀다"가 "지금 이 터미널이 활성화 상태다"를 보장하지 않는다.**

**지금 활성화 상태인지 확인하는 법**
1. 터미널 프롬프트 맨 앞에 `(.venv)` 표시가 있는지 본다 — 없으면 비활성화 상태
2. 더 확실하게: `python -c "import sys; print(sys.executable)"` 실행 →
   `...\automation\.venv\Scripts\python.exe`가 찍히면 활성화, 전역 경로(`C:\Python3xx\...`)가 찍히면 비활성화

증상은 `pytest: command not found`류의 오류. 확인·해결 순서:

1. 위 방법으로 활성화 여부 확인
2. **PyCharm 설정에서 인터프리터가 이 프로젝트의 `.venv`로 잡혀 있는지 확인**
   Settings → Project → Python Interpreter
3. **터미널에서 수동으로 activate**
   ```powershell
   .venv\Scripts\Activate.ps1    # PowerShell
   ```
   ```cmd
   .venv\Scripts\activate.bat    :: cmd.exe
   ```
   PowerShell에서 "이 시스템에서 스크립트 실행이 사용하지 않도록 설정되어 있으므로..."
   오류가 나면 실행 정책 문제다: `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`
4. 그래도 안 되면 PyCharm 터미널 대신 **외부 터미널(PowerShell 등)에서 실행**하는 게 가장 빠른 우회다 — `run_tests_and_report.bat`도 외부 터미널에서 돌리는 걸 전제로 만들었다.

> 참고: Claude Code가 명령을 실행할 때 쓰는 Bash(Git Bash) 툴은 PyCharm의 activate 설정과
> 완전히 무관한 별도 셸이라, 거기서 `pytest: command not found`가 나는 건 위 문제와는
> 다른 원인이다 — `.venv/Scripts/python.exe -m pytest`처럼 venv의 python을 직접 지정해 실행한다.

## 요구사항 해석 원칙

- 기획서·TC·화면별 테스트 노트에 명시되지 않은 정책이나 기대 결과를 임의로 추가하지 않는다. "일반적인 웹 UX라면 이렇게 동작할 것"이라는 추측만으로 검증 조건을 만들지 않는다.
- 요구사항이 불명확하거나 TC와 실제 화면이 다르면(예: TC 문서의 리셀러 목록과 dev 환경 실제 목록이 다른 경우처럼) 임의로 하나를 정답으로 정하지 말고, 그 차이를 코드 주석과 code-notes에 남겨 확인이 필요한 사항으로 분리한다.
- 기존 자동화 코드의 동작을 그 자체로 요구사항으로 간주하지 않는다. 기존 코드와 TC/기획서가 다르면 그 차이를 확인 대상으로 남긴다.

## 문서화 — 노트는 화면 단위로 분리

- 화면/기능별 세부 설명과 의사결정 배경은 `docs/notes/code-notes/<화면명>-테스트-노트.md`에 남긴다. 테스트 파일 상단에는 그 노트로의 참조 한 줄만 남긴다.
- README의 TC 표는 항상 최신 상태로 유지한다 (TC 번호, 내용, 유형).
- 개념 설명(fixture, scope, codegen 등)은 `docs/notes/자동화-테스트-노트.md`에 이미 정리돼 있으니 중복 작성하지 않는다.
- **생성 시점**: 새 화면을 다루는 테스트 파일을 추가하면, 그와 대응하는 `code-notes` 노트 파일도 같은 작업 안에서 만든다.
- **수정 시점**: 검증 로직·셀렉터 전략·트레이드오프 판단(예: 표본 페이지 뽑는 방식, skip 조건)이 바뀌면 코드만 고치고 끝내지 않고 관련 노트도 같이 갱신한다. TC 문서와 실제 화면이 다른 걸 새로 발견했을 때도 마찬가지로 노트에 반영한다. 이때 무엇을 바꿨는지뿐 아니라 **왜 바꿨는지(이전 방식의 문제, 바꾼 이유)**를 반드시 함께 남긴다 — 나중에 다시 볼 때 "왜 이렇게 했더라"를 코드만 보고는 알 수 없기 때문이다.
- 노트의 형식·분량은 자유롭다 — 화면마다 필요한 만큼만 쓰면 되고, 고정 템플릿을 채우려 하지 않는다.

### 긴 주석은 노트로 옮기고 코드에는 포인터만 남긴다 (2026-09-10 추가)

**주석이 20~50줄이 되면 코드가 안 읽힌다.** 같은 내용이 노트에도 있으면 두 벌이 되어 한쪽만 고쳐지기도 한다. 그래서 코드 주석·docstring은 **3줄 내외**로 두고, 경위·실측 근거·before/after는 노트에 두고 가리킨다.

| 코드에 남기는 것 | 노트로 보내는 것 |
|---|---|
| **무엇을** 하는가 (한 줄) | 그렇게 된 **경위**·실측 날짜·수치 |
| 지금 당장 안 지키면 사고 나는 것 (`★` 한두 줄) | 대안을 왜 버렸는지, 시도했다 실패한 것 |
| 함수 시그니처로 안 드러나는 **전제** | DOM 구조·화면 캡처·표 |

포인터는 **파일 경로 + 큰따옴표 안의 섹션 제목**으로 쓴다. 줄 번호는 쓰지 않는다 — 내용이 조금만 바뀌어도 어긋난다.

```python
# 나쁨 — 경위까지 다 적어 22줄
"""지점 목록이 새 업체 것으로 갈릴 때까지 기다린다.

2026-09-09 실측 - 업체를 바꿔도 지점 드롭다운은 즉시 안 갈린다. 이전 업체의 옵션
목록과 선택값(aria-selected="true")을 그대로 들고 있다가 …(18줄 더)
"""

# 좋음 — 무엇을·전제·★ 만 남기고 경위는 가리킨다
"""지점 목록이 **새 업체 것으로 갈리고 안정될 때까지** 기다린다.

업체를 바꿔도 즉시 안 갈린다. 라벨도 `aria-selected` 도 갈리기 전에 이미 채워져 있어
**"목록이 바뀌었다" 가 유일하게 남는 신호**다.
경위: docs/notes/code-notes/차량업체변경-테스트-노트.md "② 업체를 바꿔도 지점 드롭다운이 즉시 안 갈린다"
"""
```

**★ 옮기기 전에 그 내용이 노트에 있는지 확인한다. 없으면 노트에 먼저 쓴다.** 없는데 지우면 근거가 통째로 사라진다 — 이 저장소의 주석은 대부분 **깨지고 나서** 쓴 것이라 지우면 같은 것을 또 밟는다.

**포인터는 글자로 박혀 있어서 문서 이름·제목이 바뀌면 조용히 거짓이 된다.** 코드는 멀쩡히 돌고 테스트도 전부 통과하므로 눈으로는 못 잡는다. 문서 제목을 바꾼 뒤, 새 포인터를 넣은 뒤 돌린다.

```bash
python tools/check_doc_pointers.py
```

## 새 TC 설계 시 체크리스트

1. 로그인은 `logged_in_page`로 받는다 — 직접 구현 안 함
2. 여러 탭/조건에 동일 검증이 필요하면 parametrize
3. 목록 검증은 표본 페이지 + (표본 안에서) 전수 확인
4. 컬럼/셀렉터는 위치 하드코딩 대신 텍스트 기반 탐색
5. GIVEN/WHEN/THEN docstring + `test_<영문 설명>` 이름 (TC ID는 함수명이 아니라 `@allure.label("testcase", ...)`에만)
6. 화면별 세부 배경은 code-notes에, 여기(CLAUDE.md)는 건드리지 않음
7. 이 테스트가 다른 TC의 결과나 실행 순서에 의존하지 않는가?
8. 기대 결과가 TC/기획서/테스트 노트에 근거하고 있는가, 임의로 추측한 게 아닌가?
9. `pytest.skip`을 쓴다면 "검증 불가"이지 "결과 0건"을 감추는 게 아닌가?
10. locator가 위치나 바뀔 수 있는 텍스트(건수 등)에 과도하게 의존하지 않는가?
11. 목록 검증에서 "없어야 할 게 섞이지 않았는가"뿐 아니라 "있어야 할 게 빠지지 않았는가"도 확인했는가?
12. **이 검증 대상이 망가졌다고 가정하면 이 테스트가 빨간불이 되나?** (위 `위양성` 장 참고)
13. 루프를 돌린다면 0회 돌 수 있나 — `assert rows` 같은 하한이 있는가?
14. 부정 검증("없다")이라면 목록이 안정된 뒤 읽고, 목록이 비면 실패로 보는가?
15. **★ 데이터를 바꾸는 버튼([저장]·[제출]·[삭제] 등)을 누르지 않는가?** 노출·활성 확인까지만인가
16. 셀렉터 문자열이 테스트 파일에 직접 박혀 있지 않고 `pages/`의 화면 객체에 있는가?
17. 행을 식별하려 하고 있다면 — 전수 검사로 바꾸면 식별 자체가 필요 없어지지 않는가?
18. `expect()`가 아닌 조회 메서드(`is_visible`·`count`·`inner_text`)를 쓴다면, 그 앞에 렌더 완료를 확인하는 `expect()`가 있는가?
19. 화면의 값·구조·타이밍을 단정한 곳이 있다면, 그 근거가 **실측**인가 (기존 코드의 표현을 옮긴 게 아닌가)?
